@model string
@{
    ViewData["Title"] = "Erro";
}

<div class="text-center mt-5">
    <h4 class="text-muted">@Model</h4>
</div>

<div class="text-center mt-5">
    <a asp-controller="@ViewBag.Controller" asp-action="Index">Navegar até a página.</a>
</div>